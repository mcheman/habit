export default function LevelIndicator({name, level}) {
    return (
        <>
            <span>
                {name}
            </span>
            <span>
                {level}
            </span>
        </>
    );
}
